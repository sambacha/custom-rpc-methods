
# The URL the site will be built for
base_url = "https://www.web3rpc.io"

title = "Custom Ethereum RPC Methods"
description = "A specification for managing changes to custom EVM RPC Methods"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
highlight_theme = "css"

# This can be used to *generate* the themes, but for convenience they are simply
# generated once and then checked in.
# highlight_themes_css = [
  # { theme = "Tomorrow", filename = "syntax-theme-light.css" },
  # { theme = "charcoal", filename = "syntax-theme-dark.css" },
# ]

[extra]
# Put all your custom variables here